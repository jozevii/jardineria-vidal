---
import { Image } from "astro:assets";
import jardinImagen from "~/assets/jardin.webp";

const widths = [450, 800];
const sizes = "(min-width: 640px) 42vw, 67vw";
---

<section class="relative h-full bg-black">
  <div class="overlay opacity-50">
    <Image
    class="absolute inset-0 w-full h-full object-cover"
    src={jardinImagen}
    widths={widths}
    sizes={sizes}
    loading="eager"
    alt="A floating astronaut in a space suit"
  />
  </div>
  <div class="relative grid h-full place-items-center px-3 sm:px-0">
      <h1
      class="flex flex-col sm:items-start items-center gap-2 self-center sm:gap-4 sm:self-auto sm:justify-self-start sm:pl-10"
    >
    <div id="titulo-principal"
        class="sm:text-left text-center font-extrabold text-9xl sm:text-7xl bg-gradient-to-r font-protest-riot from-yellow-400 via-green-400 to-green-600 bg-clip-text text-transparent">
        Jardineria Vidal
    </div>
      <div
        class="text-emerald-50 sm:text-left text-center font-bold tracking-tighter text-6xl">
        Desde la tierra 
        <br /> hasta la madera, 
        <br /> cada detalle cuenta
        <br /> <span class="text-3xl font-normal">Castalla (Alicante)</span>
      </div>
    </h1>
  </div>
</section>

<noscript>
  <style>
    #splash-bg-fallback {
      display: block;
    }
  </style>
</noscript>

<style>
  #titulo-principal {
    font-family: "Arvo", serif;
  }
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }

    100% {
      transform: translate3d(0, 30px, 0);
    }
  }

  #astronaut {
    animation: float linear 2.5s infinite alternate;
  }

  @media (prefers-reduced-motion: reduce) {
    #astronaut {
      @apply animate-none;
    }

    #splash-bg-fallback {
      @apply block;
    }
  }
</style>
