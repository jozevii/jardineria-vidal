export interface Servicio {
  description: string;
  icon: string;
  title: string;
}

export interface Social {
  description: string;
  icon: string;
  url: string;
}

export interface NavItem {
  title: string;
  url: string;
}
